n = 31; % размер вектора-столбца
m = 14; % размер вектора-строки

% Вызов функции
[meanVal, varVal] = func2(n, m);

% Вывод результатов
fprintf('Среднее значение матрицы: %.4f\n', meanVal);
fprintf('Дисперсия матрицы: %.4f\n', varVal);